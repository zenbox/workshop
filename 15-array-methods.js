// - - - - - - - - - -
setTheme(title = 'array methods');
// - - - - - - - - - -
if (eval(themes[title])) {
    // - - - - - - - - - -

    // Array.of()
    // Array.from()
    // Array find()
    // Array findIndex()

    // - - - - - - - - - -
}