doctype html
html
    head
        title Data Driven Example
    body
        h1 Data Driven Example 
        
        ol
            each entry in list
                li= entry.sheep 

        h2 JSON Output
        button get some data
        output some output goes here ...


        h2 Some Markdown
        :markdown-it
            ### Look mum, no markup!
            Written in nice and short markdown syntax. Maybe
            the multiline thing istn't as bad as in pug.

            - One
            - Two
            - Three

            |One|Two|Three|
            |--|--|--|
            |Hey|Cool|Nice|

            Inline `code` in a paragraph.

            ```html
            <h1>Test</h1>
            ```
        style
            | table, th, td { border: 1px solid black; }
            | button, output { display: block; margin-bottom: 1rem;}

        script(type="module" src="/assets/js/script.js") 